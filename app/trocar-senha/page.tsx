"use client";import "../trial-manager.css";import{FormEvent,useState}from"react";export default function ChangePassword(){const[p1,setP1]=useState("");const[p2,setP2]=useState("");const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);const tenant=typeof window!=="undefined"?location.hostname.split(".")[0]||"":"";const submit=async(e:FormEvent)=>{e.preventDefault();if(p1!==p2){setMessage("As senhas não conferem.");return}setLoading(true);const r=await fetch("/api/auth/change-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({password:p1})});const j=await r.json();setLoading(false);if(!r.ok){setMessage(j.error||"Falha ao alterar senha.");return}location.href="/"};return <main className="tm-page"><div className="tm-shell"><section className="tm-card" style={{maxWidth:520,margin:"70px auto"}}><h1>Crie sua senha de acesso</h1><p>Por segurança, a senha temporária só pode ser usada no primeiro acesso. Use no mínimo 10 caracteres, com maiúscula, minúscula e número.</p><form className="tm-form" onSubmit={submit}><label className="wide">Nova senha<input type="password" minLength={10} required value={p1} onChange={e=>setP1(e.target.value)}/></label><label className="wide">Confirmar senha<input type="password" minLength={10} required value={p2} onChange={e=>setP2(e.target.value)}/></label>{message&&<div className="tm-message error">{message}</div>}<div className="tm-actions"><button className="tm-btn primary" disabled={loading}>{loading?"Salvando...":"Salvar e entrar"}</button></div></form></section></div></main>}
