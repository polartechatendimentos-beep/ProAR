"use client";

import "./public-work-map.css";
import { AlertTriangle, Building2, Camera, History, House, ImageIcon, LogIn, RefreshCw, Send, X } from "lucide-react";
import { use, useEffect, useState } from "react";
import { WORK_STATUSES, getWorkProgress, getWorkStatusColor, normalizeWorkStatus } from "../../../lib/work-status";

type StagePhoto = { label: string; url: string };
type Update = { id: string; status: string; note?: string; responsible?: string; photo?: string; photos?: string[] | StagePhoto[]; createdAt: string };
type Incident = { id: string; type: "Perda" | "Roubo"; note?: string; photo: string; responsible: string; createdAt: string };
type Observation = { id: string; author: string; role: string; houseId: string; environment?: string; stage?: string; description: string; photos?: string[]; createdAt: string; status: string };
type WorkHouse = { id: string; block: string; lot: number; kind?: "house" | "common"; name?: string; status: string; note?: string; updatedAt?: string; photo?: string; history?: Update[]; incidents?: Incident[]; externalObservations?: Observation[] };
type PublicMap = { title: string; workName?: string; houses: WorkHouse[]; updatedAt: string };
const statuses = WORK_STATUSES;
const normalizeStatus = normalizeWorkStatus;
const statusColor = getWorkStatusColor;
const updatePhotos = (update: Update): StagePhoto[] => (update.photos?.length ? update.photos : update.photo ? [update.photo] : []).map((photo,index) => typeof photo === "string" ? { label:`Foto ${index + 1}`,url:photo } : photo);

export default function PublicWorkMap({ params }: { params: Promise<{ token: string }> }) {
  const { token } = use(params);
  const [map, setMap] = useState<PublicMap | null>(null);
  const [selected, setSelected] = useState<WorkHouse | null>(null);
  const [error, setError] = useState("");
  const [username, setUsername] = useState("");
  const [password, setPassword] = useState("");
  const [externalAccess, setExternalAccess] = useState<{ name: string; role: string } | null>(null);
  const [authError, setAuthError] = useState("");
  const [authLoading, setAuthLoading] = useState(false);
  const [environment, setEnvironment] = useState("");
  const [stage, setStage] = useState("");
  const [description, setDescription] = useState("");
  const [observationPhotos, setObservationPhotos] = useState<string[]>([]);
  const [observationNotice, setObservationNotice] = useState("");

  const load = async () => { try { const response = await fetch(`/api/public-work-map?token=${encodeURIComponent(token)}`, { cache: "no-store" }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setMap(result.map); setError(""); } catch (loadError) { setError(loadError instanceof Error ? loadError.message : "Não foi possível carregar a obra."); } };
  useEffect(() => { void load(); const timer = window.setInterval(() => void load(), 30000); return () => window.clearInterval(timer); }, [token]);
  const authenticate = async () => { setAuthLoading(true); setAuthError(""); try { const response = await fetch("/api/public-work-map/access", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ token, username, password }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setExternalAccess(result.access); setMap(result.map); } catch (authLoadError) { setAuthError(authLoadError instanceof Error ? authLoadError.message : "Não foi possível autenticar."); } finally { setAuthLoading(false); } };
  const readPhoto = (file?: File) => { if (!file) return; const reader = new FileReader(); reader.onload = () => setObservationPhotos(current => [...current, String(reader.result)]); reader.readAsDataURL(file); };
  const submitObservation = async () => { if (!selected || !description.trim() || !externalAccess) return; setObservationNotice("Enviando apontamento..."); try { const response = await fetch("/api/public-work-map/access", { method:"POST", headers:{"Content-Type":"application/json"}, body:JSON.stringify({ token, username, password, houseId:selected.id, environment, stage, description, photos:observationPhotos }) }); const result = await response.json(); if (!response.ok) throw new Error(result.error); setMap(result.map); setSelected(result.map.houses.find((house: WorkHouse) => house.id === selected.id) ?? selected); setDescription(""); setEnvironment(""); setStage(""); setObservationPhotos([]); setObservationNotice("Apontamento registrado. A equipe interna fará o tratamento."); } catch (submitError) { setObservationNotice(submitError instanceof Error ? submitError.message : "Não foi possível registrar o apontamento."); } };
  const blocks = [...new Set(map?.houses.map(house => house.block) ?? [])];
  const completed = map?.houses.filter(house => normalizeStatus(house.status) === "SERVIÇO CONCLUÍDO").length ?? 0;
  const houseProgress = (house: WorkHouse) => getWorkProgress(house.status);
  const progress = map?.houses.length ? Math.round(map.houses.reduce((total,house)=>total+houseProgress(house),0)/map.houses.length) : 0;
  const blockSummary = (block: string) => { const houses = map?.houses.filter(house => house.block === block) ?? []; const done = houses.filter(house => normalizeStatus(house.status) === "SERVIÇO CONCLUÍDO").length; const inProgress = houses.filter(house => houseProgress(house) > 0 && houseProgress(house) < 100).length; const pending = houses.length - done - inProgress; const average = houses.length ? Math.round(houses.reduce((sum, house) => sum + houseProgress(house), 0) / houses.length) : 0; return { houses, completed:done, inProgress, pending, average }; };
  const workName = map?.workName || map?.title?.split("—").pop()?.trim() || "Obra";
  return <main className="public-map-page"><header><img src="/proar-logo.png" alt="ProAR"/><div><span>ACOMPANHAMENTO EM TEMPO REAL</span><h1>{workName}</h1><p>Atualização automática a cada 30 segundos • consulte o andamento de cada unidade.</p></div><button onClick={load}><RefreshCw size={16}/> Atualizar</button></header>{error ? <section className="public-map-error">{error}</section> : !map ? <section className="public-map-loading">A carregar o andamento da obra...</section> : <><section className="public-work-name"><span>OBRA</span><strong>{workName}</strong><small>{map.houses.length} unidades acompanhadas pela PolarTech</small></section><section className="public-map-summary"><div><small>PROGRESSO GERAL</small><strong>{progress}%</strong><i><b style={{width:`${progress}%`}}/></i></div><span>Avanço calculado por etapa • {completed} de {map.houses.length} unidades finalizadas</span><time>Última atualização: {new Date(map.updatedAt).toLocaleString("pt-BR")}</time></section><section className="external-access-banner"><div><b>{externalAccess ? `Olá, ${externalAccess.name}` : "Acesso de engenheiro ou fiscal"}</b><small>{externalAccess ? `${externalAccess.role} • registre apontamentos sem alterar o andamento da obra.` : "Entre para registrar observações técnicas, etapa, ambiente e fotos."}</small></div>{externalAccess ? <button onClick={()=>setExternalAccess(null)}><LogIn size={14}/> Sair</button> : <button onClick={()=>document.getElementById("external-login")?.scrollIntoView({behavior:"smooth"})}><LogIn size={14}/> Entrar</button>}</section><nav>{statuses.map(status => <span key={status}><i style={{background:statusColor(status)}}/>{status}</span>)}</nav><div className="public-blocks">{blocks.map(block => { const summary=blockSummary(block); return <section key={block}><header><div><b>{block === "Áreas Comuns" ? "ÁREAS COMUNS" : `QUADRA ${block}`}</b><small>{summary.houses.length} unidades • {summary.completed} concluídas • {summary.inProgress} em execução • {summary.pending} pendentes</small><div className="public-block-progress"><i><b style={{width:`${summary.average}%`}}/></i><strong>{summary.average}%</strong></div></div><span>{summary.completed}/{summary.houses.length} concluídas</span></header><div>{summary.houses.map(house => <article key={house.id} style={{"--status":statusColor(house.status)} as React.CSSProperties} onDoubleClick={() => setSelected(house)}><span>{house.kind === "common" ? <Building2 size={17}/> : <House size={17}/>}</span><h2>{house.kind === "common" ? house.name : `Casa ${String(house.lot).padStart(2,"0")}`}</h2><p><i/>{normalizeStatus(house.status)}</p><small>{house.updatedAt ? new Date(house.updatedAt).toLocaleString("pt-BR") : "Sem atualizações"}</small><div className="public-house-progress"><i><b style={{width:`${houseProgress(house)}%`}}/></i><span>{houseProgress(house)}%</span></div>{house.photo && <img src={house.photo} alt={house.name || `Casa ${house.lot}`}/>} {house.incidents?.length ? <em className="public-incident-badge"><AlertTriangle size={11}/>{house.incidents.length} ocorrência(s)</em> : null}<button onClick={() => setSelected(house)}><History size={13}/> Ver histórico</button></article>)}</div></section>; })}</div></>}{!externalAccess && <section id="external-login" className="external-login-card"><div><span>ÁREA RESTRITA</span><h2>Engenheiro / Fiscal</h2><p>Use o login fornecido pela equipe interna. O acesso não permite alterar status, progresso, custos, materiais ou histórico interno.</p></div><div className="external-login-form"><input value={username} onChange={event=>setUsername(event.target.value)} placeholder="Login" autoComplete="username"/><input type="password" value={password} onChange={event=>setPassword(event.target.value)} placeholder="Senha" autoComplete="current-password"/><button onClick={()=>void authenticate()} disabled={authLoading}><LogIn size={14}/>{authLoading ? "Validando..." : "Entrar"}</button></div>{authError && <small className="external-error">{authError}</small>}</section>}{selected && <div className="public-history-layer"><button aria-label="Fechar" onClick={() => setSelected(null)}/><section><header><div><span>{selected.kind === "common" ? "ÁREA COMUM" : `QUADRA ${selected.block}`}</span><h2>{selected.kind === "common" ? selected.name : `Casa / lote ${String(selected.lot).padStart(2,"0")}`}</h2></div><button onClick={() => setSelected(null)}><X size={17}/></button></header><div className="public-history-list">{selected.history?.length ? [...selected.history].reverse().map(update => <article key={update.id}><i style={{background:statusColor(update.status)}}/><div><header><b>{normalizeStatus(update.status)}</b><time>{new Date(update.createdAt).toLocaleString("pt-BR")}</time></header><p><strong>Responsável: {update.responsible || "Não informado"}</strong><br/>{update.note || "Sem observações."}</p><div>{updatePhotos(update).map(photo => <figure key={photo.label}><img src={photo.url} alt={photo.label}/><figcaption>{photo.label}</figcaption></figure>)}</div></div></article>) : <p className="empty-history"><ImageIcon size={22}/>Nenhuma alteração registrada.</p>}</div>{externalAccess && <section className="external-observation-form"><header><div><b>Novo apontamento externo</b><small>Este registro será recebido pela equipe interna, sem alterar a etapa da obra.</small></div><Send size={16}/></header><label>Ambiente / sala<input value={environment} onChange={event=>setEnvironment(event.target.value)} placeholder="Ex.: Sala técnica, cozinha, banheiro"/></label><label>Etapa relacionada<input value={stage} onChange={event=>setStage(event.target.value)} placeholder="Ex.: Instalação, vistoria, acabamento"/></label><label>Descrição<textarea value={description} onChange={event=>setDescription(event.target.value)} placeholder="Descreva o apontamento técnico..."/></label><label className="external-photo-input"><Camera size={16}/> Adicionar foto<input type="file" accept="image/*" multiple onChange={event=>Array.from(event.target.files ?? []).slice(0,8).forEach(readPhoto)}/></label>{observationPhotos.length > 0 && <small>{observationPhotos.length} foto(s) pronta(s) para envio.</small>}<button className="external-submit" disabled={!description.trim()} onClick={()=>void submitObservation()}><Send size={14}/> Enviar apontamento</button>{observationNotice && <small className="external-notice">{observationNotice}</small>}</section>}{selected.externalObservations?.length ? <section className="external-observations"><header><b>Apontamentos externos desta unidade</b><small>{selected.externalObservations.length} registro(s)</small></header>{selected.externalObservations.map(observation=><article key={observation.id}><b>{observation.author} • {observation.role}</b><time>{new Date(observation.createdAt).toLocaleString("pt-BR")}</time><p>{observation.description}</p>{observation.environment && <small>Ambiente: {observation.environment}</small>}{observation.stage && <small>Etapa: {observation.stage}</small>}{observation.photos?.map((photo,index)=><img key={index} src={photo} alt={`Foto do apontamento ${index + 1}`}/>)}</article>)}</section> : null}{selected.incidents?.length ? <section className="public-incidents"><header><AlertTriangle size={15}/><div><b>Perdas e Roubos</b><small>{selected.incidents.length} ocorrência(s) registrada(s)</small></div></header><div>{selected.incidents.map(incident=><article key={incident.id}><img src={incident.photo} alt={incident.type}/><div><strong>{incident.type}</strong><time>{new Date(incident.createdAt).toLocaleString("pt-BR")}</time><p>{incident.note || "Sem observações."}</p><small>Responsável: {incident.responsible}</small></div></article>)}</div></section> : null}</section></div>}</main>;
}
