"use client";

import { useMemo, useState } from "react";
import { Building2, CalendarDays, CheckCircle2, ChevronRight, ClipboardList, Edit3, FileText, Filter, History, MapPin, Plus, Save, Search, ShieldCheck, UserRound, Wrench, X } from "lucide-react";
import "./customer-profile.css";

type AnyRecord = Record<string, unknown>;
type CustomerRecord = AnyRecord & { id: string; name: string; doc?: string; contact?: string; phone?: string; address?: string; units?: number; status?: string; organizationType?: string; legalName?: string; tradeName?: string; email?: string; zipCode?: string; street?: string; addressNumber?: string; complement?: string; neighborhood?: string; city?: string; state?: string; stateRegistration?: string; municipalRegistration?: string; createdAt?: string; website?: string; segment?: string; size?: string; paymentCondition?: string; priceTable?: string; preferredPaymentMethod?: string; dueDay?: string | number; financialNote?: string };
type ServiceOrderRecord = AnyRecord & { id: string; client: string; unit: string; service: string; tech: string; date: string; time?: string; status: string; address?: string; timeline?: AnyRecord[] };
type Props = { customer: CustomerRecord; structures: AnyRecord[]; serviceOrders: ServiceOrderRecord[]; modules: Record<string, AnyRecord[]>; canEdit: boolean; onBack: () => void; onOpen: (name: string) => void; onUpdateCustomer: (customer: CustomerRecord) => void; onUpdateStructure: (record: AnyRecord) => void };

type Tab = "Cadastro" | "Unidades / Secretarias / Áreas" | "Setores / Ambientes / Salas" | "Histórico da Sala / Ambiente" | "Equipamentos" | "Histórico";
const tabs: Tab[] = ["Cadastro", "Unidades / Secretarias / Áreas", "Setores / Ambientes / Salas", "Histórico da Sala / Ambiente", "Equipamentos", "Histórico"];
const value = (record: AnyRecord | undefined, ...keys: string[]) => { for (const key of keys) { const item = record?.[key]; if (item !== undefined && item !== null && String(item).trim()) return String(item); } return "—"; };
const related = (record: AnyRecord, customer: CustomerRecord) => { const names = [customer.name, customer.legalName, customer.tradeName].filter(Boolean).map(String); return names.includes(String(record.client ?? record.customer ?? record.customerName ?? "")); };
const dateLabel = (raw: unknown) => { if (!raw) return "—"; const date = new Date(String(raw)); return Number.isNaN(date.getTime()) ? String(raw) : date.toLocaleDateString("pt-BR"); };

export function CustomerProfileWorkspace({ customer, structures, serviceOrders, modules, canEdit, onBack, onOpen, onUpdateCustomer, onUpdateStructure }: Props) {
  const [tab, setTab] = useState<Tab>("Cadastro");
  const [structureType, setStructureType] = useState("Todos");
  const [roomQuery, setRoomQuery] = useState("");
  const [roomId, setRoomId] = useState("");
  const [equipmentFilters, setEquipmentFilters] = useState({ unit: "Todos", area: "Todos", room: "Todos", type: "Todos", brand: "Todos", status: "Todos", pmoc: "Todos", warranty: "Todos" });
  const [historyFilter, setHistoryFilter] = useState("Todos");
  const [draft, setDraft] = useState<CustomerRecord>(customer);
  const customerStructures = useMemo(() => structures.filter(item => related(item, customer)), [structures, customer]);
  const rooms = useMemo(() => customerStructures.filter(item => /sala|ambiente|consult|labor|farm|uti|recep|almox|audit|cozinha|cpd|quarto/i.test(`${value(item, "name")} ${value(item, "category")} ${value(item, "environmentType", "tipoAmbiente")}`)), [customerStructures]);
  const equipment = useMemo(() => (modules["Equipamentos"] ?? []).filter(item => related(item, customer)), [modules, customer]);
  const selectedRoom = rooms.find(item => String(item.id) === roomId) ?? rooms[0];
  const roomEquipment = selectedRoom ? equipment.filter(item => value(item, "unit", "equipmentUnit", "parentUnit", "installationLocation") === value(selectedRoom, "name", "unit") || value(item, "room", "environment", "ambiente") === value(selectedRoom, "name", "unit")) : [];
  const allHistory = useMemo(() => {
    const orders = serviceOrders.filter(order => order.client === customer.name).flatMap(order => [{ id: order.id, date: order.date, type: "OS", title: `${order.id} criada`, detail: `${order.unit} → ${order.service}`, record: order }, ...(order.timeline ?? []).map(event => ({ id: `${order.id}-${String(event.id)}`, date: String(event.createdAt ?? order.date), type: "OS", title: String(event.status ?? "Atualização da OS"), detail: String(event.internalNote ?? event.customerNote ?? order.service), record: order }))]);
    const records = Object.entries(modules).flatMap(([module, list]) => list.filter(item => related(item, customer)).map(item => ({ id: item.id, date: String(item.date ?? item.createdAt ?? ""), type: module, title: `${module} • ${item.name}`, detail: String(item.description ?? item.status ?? "Registro cadastrado"), record: item })));
    return [...orders, ...records].sort((a, b) => String(b.date).localeCompare(String(a.date)));
  }, [customer, modules, serviceOrders]);
  const filteredHistory = allHistory.filter(item => historyFilter === "Todos" || item.type.toLowerCase().includes(historyFilter.toLowerCase()) || (historyFilter === "Alterações cadastrais" && item.type === "Cadastro"));
  const updateDraft = (key: string, next: unknown) => setDraft(current => ({ ...current, [key]: next }));
  const saveCustomer = () => onUpdateCustomer(draft);
  const structureList = customerStructures.filter(item => structureType === "Todos" || value(item, "category", "type", "organizationType") === structureType);
  const roomList = rooms.filter(item => `${value(item, "name", "unit")} ${value(item, "code", "codigo")} ${value(item, "sector", "setor")}`.toLowerCase().includes(roomQuery.toLowerCase()));
  const equipmentList = equipment.filter(item => Object.entries(equipmentFilters).every(([key, filter]) => filter === "Todos" || value(item, key === "area" ? "secretary" : key, key === "brand" ? "manufacturer" : key).toLowerCase() === filter.toLowerCase()));
  const options = (key: string) => ["Todos", ...Array.from(new Set(equipment.map(item => value(item, key)).filter(item => item !== "—")))];
  const isPublic = /Prefeitura|Órgão Público|Autarquia|Fundação|Entidade Pública/i.test(customer.organizationType ?? "");
  const tabLabel = isPublic ? "Prefeitura → Secretaria → Unidade → Setor → Sala" : "Unidades, áreas e ambientes vinculados ao cliente";

  return <section className="customer-profile-workspace">
    <header className="customer-profile-header"><button className="customer-back" onClick={onBack}><ChevronRight size={16} className="rotate-180"/> Clientes</button><div className="customer-profile-identity"><span>{customer.name.split(" ").map(part => part[0]).slice(0, 2).join("")}</span><div><small>PERFIL 360° DO CLIENTE</small><h2>{customer.name}</h2><p>{value(customer, "doc")} • {value(customer, "city")} / {value(customer, "state")}</p></div></div><div className="customer-profile-actions"><button className="outline-btn" onClick={() => onOpen("Relatórios")}><FileText size={14}/> Relatórios</button><button className="primary-btn" onClick={saveCustomer} disabled={!canEdit}><Save size={14}/> Salvar alterações</button></div></header>
    <nav className="customer-profile-tabs" aria-label="Abas principais do cliente">{tabs.map(item => <button key={item} className={tab === item ? "active" : ""} onClick={() => setTab(item)}>{item}</button>)}</nav>

    {tab === "Cadastro" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><UserRound size={12}/> CADASTRO</span><h3>Dados gerais do cliente</h3><p>Identificação, contato, endereço e condições financeiras.</p></div><button className="outline-btn" onClick={() => setDraft(customer)}><X size={14}/> Descartar alterações</button></div><div className="profile-section-grid">
      <article className="profile-card"><h4>Identificação</h4><div className="profile-form-grid">{[["legalName", "Razão Social"], ["tradeName", "Nome Fantasia"], ["doc", "CNPJ / CPF"], ["stateRegistration", "Inscrição Estadual"], ["municipalRegistration", "Inscrição Municipal"], ["createdAt", "Data de cadastro"], ["segment", "Segmento"], ["size", "Porte"], ["website", "Site"]].map(([key, label]) => <label key={key}>{label}<input value={value(draft, key)} onChange={event => updateDraft(key, event.target.value)} /></label>)}</div></article>
      <article className="profile-card"><h4>Contato</h4><div className="profile-form-grid">{[["phone", "Telefone"], ["whatsapp", "WhatsApp"], ["email", "E-mail"], ["contact", "Contato principal"], ["contactRole", "Cargo"], ["additionalContacts", "Contatos adicionais"]].map(([key, label]) => <label key={key}>{label}<input value={value(draft, key)} onChange={event => updateDraft(key, event.target.value)} /></label>)}</div></article>
      <article className="profile-card wide"><h4>Endereço</h4><div className="profile-form-grid">{[["zipCode", "CEP"], ["street", "Logradouro"], ["addressNumber", "Número"], ["complement", "Complemento"], ["neighborhood", "Bairro"], ["city", "Cidade"], ["state", "UF"], ["reference", "Referência"]].map(([key, label]) => <label key={key}>{label}<input value={value(draft, key)} onChange={event => updateDraft(key, event.target.value)} /></label>)}</div></article>
      <article className="profile-card wide"><h4>Financeiro</h4><div className="profile-form-grid">{[["paymentCondition", "Condição de pagamento padrão"], ["priceTable", "Tabela de preço"], ["preferredPaymentMethod", "Forma de pagamento preferencial"], ["dueDay", "Dia de vencimento"], ["financialNote", "Observação financeira"]].map(([key, label]) => <label key={key}>{label}<input value={value(draft, key)} onChange={event => updateDraft(key, event.target.value)} /></label>)}</div></article>
    </div></div>}

    {tab === "Unidades / Secretarias / Áreas" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><Building2 size={12}/> ESTRUTURA ORGANIZACIONAL</span><h3>{tabLabel}</h3><p>{isPublic ? "Organize a prefeitura e seus órgãos sem misturar secretarias, unidades ou setores." : "Unidades, filiais, áreas e locais vinculados a este cliente."}</p></div><div className="profile-action-row"><button className="outline-btn" onClick={() => onOpen(`Nova unidade, filial ou setor • ${customer.name}`)}><Plus size={14}/> Nova Unidade</button><button className="primary-btn" onClick={() => onOpen(`Novo setor • ${customer.name}`)}><Plus size={14}/> Novo Setor</button></div></div><div className="profile-filter-row"><label><Filter size={14}/> Tipo<select value={structureType} onChange={event => setStructureType(event.target.value)}><option>Todos</option>{Array.from(new Set(customerStructures.map(item => value(item, "category", "type")))).filter(item => item !== "—").map(item => <option key={item}>{item}</option>)}</select></label></div><div className="hierarchy-list">{structureList.map(item => <article className="hierarchy-card" key={String(item.id)}><div className="hierarchy-icon"><Building2 size={19}/></div><div><span>{value(item, "category", "type")}</span><h4>{value(item, "name", "unit")}</h4><p>{value(item, "parentUnit", "parent", "secretary", "area")} {value(item, "sector", "setor") !== "—" ? `→ ${value(item, "sector", "setor")}` : ""}</p></div><div className="hierarchy-meta"><small>Responsável</small><b>{value(item, "contact", "responsible")}</b><small>{value(item, "address", "city")}</small></div><button className="icon-action" onClick={() => onUpdateStructure(item)} aria-label="Abrir cadastro"><Edit3 size={15}/></button></article>)}{!structureList.length && <div className="empty-state"><Building2 size={25}/><h4>Nenhuma unidade ou área cadastrada</h4><p>Use os botões acima para iniciar a hierarquia do cliente.</p></div>}</div></div>}

    {tab === "Setores / Ambientes / Salas" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><MapPin size={12}/> LOCAIS FÍSICOS</span><h3>Setores, ambientes e salas</h3><p>Cadastre o local exato onde existem equipamentos ou serviços.</p></div><div className="profile-action-row"><button className="outline-btn" onClick={() => onOpen(`Novo setor • ${customer.name}`)}><Plus size={14}/> Novo Setor</button><button className="primary-btn" onClick={() => onOpen(`Nova sala ou ambiente • ${customer.name}`)}><Plus size={14}/> Nova Sala</button></div></div><label className="wide-search"><Search size={15}/><input value={roomQuery} onChange={event => setRoomQuery(event.target.value)} placeholder="Buscar sala, setor, código interno ou ambiente..." /></label><div className="rooms-grid">{roomList.map(room => <article className="room-card" key={String(room.id)} onClick={() => { setRoomId(String(room.id)); setTab("Histórico da Sala / Ambiente"); }}><div className="room-card-head"><span><MapPin size={17}/></span><div><small>{value(room, "environmentType", "tipoAmbiente", "category")}</small><h4>{value(room, "name", "unit")}</h4></div><ChevronRight size={16}/></div><div className="room-facts"><span><b>Unidade</b>{value(room, "unit", "parentUnit", "secretary")}</span><span><b>Setor</b>{value(room, "sector", "setor")}</span><span><b>Código</b>{value(room, "code", "codigo")}</span><span><b>Responsável</b>{value(room, "responsible", "contact")}</span></div><footer><span>{value(room, "area", "areaM2")} m²</span><span className={/prior/i.test(value(room, "priority", "criticality")) ? "priority" : ""}>{value(room, "criticality", "criticidade", "priority")}</span></footer></article>)}</div>{!roomList.length && <div className="empty-state"><MapPin size={25}/><h4>Nenhuma sala ou ambiente encontrado</h4><p>Cadastre o primeiro ambiente físico deste cliente.</p></div>}</div>}

    {tab === "Histórico da Sala / Ambiente" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><History size={12}/> HISTÓRICO DO AMBIENTE</span><h3>{value(selectedRoom, "name", "unit")}</h3><p>Equipamentos, atendimentos, transferências e observações deste local.</p></div><select className="room-selector" value={roomId || String(selectedRoom?.id ?? "")} onChange={event => setRoomId(event.target.value)}>{rooms.map(item => <option value={String(item.id)} key={String(item.id)}>{value(item, "name", "unit")}</option>)}</select></div>{selectedRoom ? <div className="room-history-grid"><article className="profile-card"><h4>Equipamentos instalados</h4>{roomEquipment.map(item => <div className="history-equipment" key={String(item.id)}><Wrench size={16}/><div><b>{value(item, "name", "model", "equipmentType")}</b><span>{value(item, "capacityBtus", "capacity", "btus")} BTUs • {value(item, "brand", "manufacturer")}</span><small>Série {value(item, "serialNumber", "serial")} • Patrimônio {value(item, "assetCode", "patrimony", "patrimonio")}</small></div></div>)}{!roomEquipment.length && <p className="muted">Nenhum equipamento vinculado a este ambiente.</p>}</article><article className="profile-card"><h4>Histórico de atendimento</h4><div className="mini-timeline">{serviceOrders.filter(order => order.client === customer.name && order.unit === value(selectedRoom, "unit", "name")).map(order => <div key={order.id}><time>{dateLabel(order.date)}</time><b>{order.service}</b><span>{order.status} • {order.tech}</span></div>)}{!serviceOrders.some(order => order.client === customer.name && order.unit === value(selectedRoom, "unit", "name")) && <p className="muted">Nenhum atendimento registrado para este ambiente.</p>}</div></article><article className="profile-card"><h4>Histórico de equipamentos</h4><div className="mini-timeline">{roomEquipment.flatMap(item => Array.isArray(item.equipmentLabelHistory) ? item.equipmentLabelHistory.map((event, index) => <div key={`${item.id}-${index}`}><time>Registro</time><b>{String(event)}</b><span>{value(item, "name", "model")}</span></div>) : []).slice(0, 12)}{!roomEquipment.some(item => Array.isArray(item.equipmentLabelHistory) && item.equipmentLabelHistory.length) && <p className="muted">Transferências e substituições aparecerão aqui.</p>}</div></article><article className="profile-card"><h4>Histórico de observações</h4><div className="observation-list">{serviceOrders.filter(order => order.client === customer.name && order.unit === value(selectedRoom, "unit", "name")).flatMap(order => [order.service, ...(order.timeline ?? []).map(event => String(event.internalNote ?? event.customerNote ?? ""))]).filter(Boolean).slice(0, 12).map((note, index) => <p key={`${note}-${index}`}><CheckCircle2 size={14}/>{note}</p>)}<p className="muted">Registros de reclamações e observações técnicas vinculados ao ambiente.</p></div></article></div> : <div className="empty-state"><MapPin size={25}/><h4>Selecione uma sala</h4><p>Cadastre ou selecione um ambiente para consultar seu histórico.</p></div>}</div>}

    {tab === "Equipamentos" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><Wrench size={12}/> ATIVOS DO CLIENTE</span><h3>Equipamentos vinculados ao cliente</h3><p>{equipmentList.length} equipamento(s) encontrado(s).</p></div><button className="primary-btn" onClick={() => onOpen(`Novo • Equipamentos`)}><Plus size={14}/> Novo equipamento</button></div><div className="equipment-filter-grid">{Object.entries(equipmentFilters).map(([key, current]) => <label key={key}>{key === "area" ? "Secretaria / Área" : key === "room" ? "Sala" : key.charAt(0).toUpperCase() + key.slice(1)}<select value={current} onChange={event => setEquipmentFilters(previous => ({ ...previous, [key]: event.target.value }))}>{options(key === "area" ? "secretary" : key)}</select></label>)}</div><div className="table-wrap profile-table"><table><thead><tr><th>CÓDIGO</th><th>EQUIPAMENTO</th><th>BTUs</th><th>MARCA</th><th>UNIDADE</th><th>SETOR</th><th>SALA</th><th>PATRIMÔNIO</th><th>STATUS</th><th /></tr></thead><tbody>{equipmentList.map(item => <tr key={String(item.id)}><td>{value(item, "id", "code")}</td><td><b>{value(item, "name", "model", "equipmentType")}</b><small>{value(item, "serialNumber", "serial")}</small></td><td>{value(item, "capacityBtus", "capacity", "btus")}</td><td>{value(item, "brand", "manufacturer")}</td><td>{value(item, "unit", "equipmentUnit", "parentUnit")}</td><td>{value(item, "sector", "setor")}</td><td>{value(item, "room", "environment", "installationLocation")}</td><td>{value(item, "assetCode", "patrimony", "patrimonio")}</td><td><span className="status-pill"><i />{value(item, "status")}</span></td><td><button className="open-link" onClick={() => onOpen(`Equipamento • ${value(item, "name", "model")}`)}>Abrir equipamento</button></td></tr>)}</tbody></table>{!equipmentList.length && <div className="empty-state compact"><Wrench size={22}/><h4>Nenhum equipamento encontrado</h4></div>}</div></div>}

    {tab === "Histórico" && <div className="profile-pane"><div className="profile-pane-head"><div><span className="section-kicker"><History size={12}/> LINHA DO TEMPO</span><h3>Histórico completo do cliente</h3><p>OS, orçamentos, financeiro, equipamentos, PMOC, obras, documentos e alterações cadastrais.</p></div><select className="room-selector" value={historyFilter} onChange={event => setHistoryFilter(event.target.value)}>{["Todos", "OS", "Orçamentos", "Financeiro", "Equipamentos", "PMOC", "Obras", "Documentos", "Alterações cadastrais"].map(item => <option key={item}>{item}</option>)}</select></div><div className="profile-timeline">{filteredHistory.map(item => <article key={`${item.type}-${item.id}`}><div className="timeline-dot"><History size={14}/></div><div><time>{dateLabel(item.date)}</time><h4>{item.title}</h4><p>{item.detail}</p><span>{item.type}</span></div></article>)}{!filteredHistory.length && <div className="empty-state"><History size={25}/><h4>Nenhum registro encontrado</h4><p>Os eventos relacionados ao cliente aparecerão aqui.</p></div>}</div></div>}
  </section>;
}

export type { CustomerRecord };
